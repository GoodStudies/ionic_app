import { Participant } from "../entity/Participant";

interface ExistingParticipantModalProps {
	id: number,
	participant: Participant,
}

const ExistingParticipantModal: React.FC<ExistingParticipantModalProps> = ({
	id,
	participant,
}) =>  {
	return (
		<>

		</>
	);
}

export default ExistingParticipantModal